Created react app
Setup frontend files
Download the mongodb community server then added the path to environment variables
Download the mongo sh then added the path to environment variables
Download the mongo command line tools then added the path to environment variables
Installed the express nodemon mongoose
Install express validator
Install bcrypt and jsonwebtoken
Install mui icon @emotion/react @emotion/styled